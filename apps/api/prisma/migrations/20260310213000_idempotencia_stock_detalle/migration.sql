BEGIN TRY

BEGIN TRAN;

ALTER TABLE [dbo].[ESTADO_STOCK]
ADD [ID_REFERENCIA_DETALLE] BIGINT NULL;

CREATE NONCLUSTERED INDEX [IX_ESTADO_STOCK_IDEMPOTENCIA]
ON [dbo].[ESTADO_STOCK](
  [ORIGEN_MOVIMIENTO],
  [ID_REFERENCIA_ORIGEN],
  [ID_REFERENCIA_DETALLE],
  [ID_ITEM_CATALOGO],
  [TIPO_MOVIMIENTO]
);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
